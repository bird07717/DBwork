# 决策记录

## 2026-06-05 DeepSeek 接入方式

- AI 助手接入 DeepSeek 的位置放在 Python `ai-service`，前端和 Java 后端保持现有调用链。
- Java 后端只透传可选 `model` 字段，不改变公开 API 响应格式。
- AI 服务优先调用 DeepSeek Chat Completions；未配置 Key、外部调用失败或返回内容解析失败时，继续返回本地规则降级结果。
- 支持模型限定为 `deepseek-v4-flash` 和 `deepseek-v4-pro`，默认使用 `deepseek-v4-flash`。
- 不引入新依赖，DeepSeek HTTP 调用使用 Python 标准库实现。
- 真实 API Key 只通过本地环境变量或被 Git 忽略的 `ai-service/.env` / `.env.local` 配置，不写入仓库文件。

## 2026-06-04 腾讯地图接入方式

- 首页地图和线路详情地图使用腾讯地图 JavaScript API GL。
- 开发阶段 API Key：`77MBZ-JJGCQ-MGF5A-BJCUR-4LKZO-JVB2W`。
- 前端统一通过 `VITE_TENCENT_MAP_KEY` 读取 Key，业务源码不硬编码 Key。
- 当前真实线路坐标按腾讯地图可用坐标处理，数据文件中的 `coordinateSystem` 标记为 `tencent`。
- 地图默认中心点为 `lat=32.045, lng=120.82`，默认缩放级别为 `14`。

## 2026-06-04 初始化账号与调度规则

- 第一版初始化管理员账号已创建，显示姓名为课程项目管理员。
- 初始密码不得明文保存到数据库，后续 SQL 或后端初始化逻辑必须写入加密后的 `password_hash`。
- 模拟车辆默认容量为 50 人。
- 早高峰为 07:00-09:00，核心高峰为 07:30-08:30。
- 晚高峰为 17:00-19:00，核心高峰为 17:30-18:30。
- 调度建议采用默认阈值：高峰满载率大于等于 85% 建议增加班次，60% 到 85% 建议维持并观察，正常时段满载率小于 30% 建议减少班次或延长发车间隔，客流高于近 7 日均值 30% 标记为异常增长。

## 2026-06-04 开发阶段边界

- 阶段 1 到阶段 6 作为开发阶段，目标是交付可本地运行、可联调、可演示版本。
- 阶段 7 作为用户自测与验收阶段，由用户自行测试、截图、整理课程报告和最终演示素材。
- 每个开发任务必须按 `docs/execution-workflow.md` 执行闭环。
- 每个大阶段结束后必须完成阶段检查验证，通过后才能进入下一阶段。

## 2026-06-04 Docker 基础设施端口

- MySQL 使用宿主机端口 `3307` 映射容器端口 `3306`，原因是当前开发环境中 `3306` 已被本机 MySQL 占用。
- Redis 容器端口仍为 `6379`，宿主机端口使用 `6380`，原因是当前开发环境中 `6379` 已被占用。
- 后端本地连接 Redis 时应使用 `localhost:6380`。
- 后端本地连接 MySQL 时应使用 `localhost:3307`。

## 2026-06-04 第一版管理员密码哈希

- 第一版管理员初始密码不明文入库。
- `admin_user.password_hash` 使用 SHA-256 十六进制字符串。
- 哈希输入格式为 `bus-agent:{username}:{plainPassword}`。
- 初始化管理员密码哈希已写入 `sql/init_admin.sql`。
- 后续 Java 登录接口必须按同一规则计算哈希并比对。
