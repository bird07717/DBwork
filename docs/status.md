# 当前项目状态

## 2026-06-05

- 已修复基础数据页白屏问题：字段中文化时 `labels` 定义晚于 `metas` 初始化导致浏览器运行时 TDZ 异常，已调整初始化顺序；同时在根组件增加运行时错误兜底，页面异常时显示错误提示而不是空白主区域。
- 已修复前端自测 BUG：模拟数据按班次生成并限制单班不超过 50 人，数据库验证最高满载率为 100%；首页、线路详情、客流分析、AI 助手、后台首页和调度建议页补充加载/错误兜底；侧边栏品牌标识改为线路节点图形。
- 已完成前端体验和模拟客流数据可信度修正：AI 助手改为“本地规则分析”表达，后台基础数据字段中文化，游客端页面补充跳转闭环，侧边栏移除“演示环境”提示并重做品牌导航，模拟乘车数据提升至 90000 条；默认 2026-05-29 至 2026-06-04 区间早高峰班均客流约 44-46 人、早高峰平均满载率约 88%-91%。
- 已完成侧边栏和其他页面前端视觉统一：全局侧边栏分组展示，线路详情增加指标和站点联动，客流分析增加筛选面板和指标摘要，AI 助手改为决策报告布局，后台首页/基础数据/调度建议/登录页完成视觉增强。
- 已完成首页地图驱动第一阶段改造：首屏改为地图态势页，增加左上运行态势浮层、今日总客流、最高满载率、告警摘要、右侧线路/站点状态面板，并保留腾讯地图 Key 缺失降级视图。
- 阶段 7 用户自测问题已修复：地图站点标记恢复为较小的图钉样式，并按客流强度显示红色（高）、黄色（正常）、绿色（低）。
- 首页地图和线路详情地图已接入近 7 日站点客流数据，站点标记可基于当前线路的相对客流分布变色。
- 客流分析高峰对比已改为按班次聚合，展示班均客流和平均满载率，避免仅用总量比较导致高峰表现不明显。
- 模拟乘车数据已按早晚高峰加权重新生成并导入：30 辆车、30 名司机、3000 条班次、10000 条乘车记录；默认日期范围内 3 条线路早晚高峰班均客流均高于正常时段。

## 2026-06-04

- 需求分析与技术选型阶段已结束。
- 项目进入任务拆分阶段。
- 已确定第一版 MVP 页面范围：游客：首页地图、线路详情、客流分析、AI 助手；管理员：登录页、后台首页、基础数据管理、调度建议管理。
- 已完成文档：需求文档、MVP 文档、数据库设计文档、腾讯地图接入文档、任务执行规范、阶段任务拆分。
- 已整理 3 条真实公交线路数据到 `data/real_routes/`。
- 已完成阶段 1 的 T1.1 项目目录结构创建：`frontend/`、`backend/`、`ai-service/`、`sql/`、`scripts/`。
- 已完成阶段 1 的 T1.2 Docker Compose 基础设施：`docker-compose.yml` 已创建，MySQL 8.0 和 Redis 7 可通过 WSL Docker Compose 启动并显示 healthy。
- 已完成阶段 1 的 T1.3 环境变量示例：`frontend/.env.example`、`backend/.env.example`、`ai-service/.env.example` 已创建。
- 已完成阶段 1 的 T1.4 README 初版：`README.md` 已创建，包含项目定位、技术栈、目录结构和基础启动方式。
- 阶段 1 已完成并通过检查：目录结构完整，Docker Compose 配置有效，MySQL 与 Redis 可启动并显示 healthy，环境变量示例和 README 均已存在。
- 已完成阶段 2 的 T2.1 数据库 schema SQL：`sql/schema.sql` 已创建并在 Docker MySQL 中执行成功，10 张核心表、外键和关键索引均创建成功。
- 已完成阶段 2 的 T2.2 管理员初始化 SQL：`sql/init_admin.sql` 已创建并执行成功，`wjhadmin` 账号存在，`password_hash` 非明文。
- 已完成阶段 2 的 T2.3 真实线路导入脚本：`scripts/import_route_data.py` 已创建并执行成功，3 条线路站点数量分别为 27、17、32，站序连续无缺口。
- 已完成阶段 2 的 T2.4 模拟数据生成脚本：`scripts/generate_mock_data.py` 已创建并执行成功，生成 30 辆车、30 名司机、3000 条班次和 10000 条乘车记录，乘车记录包含 `morning_peak`、`evening_peak`、`normal` 三类时段。
- 已完成阶段 2 的 T2.5 核心统计 SQL 验证：`sql/queries/route_flow.sql`、`station_flow.sql`、`peak_analysis.sql`、`load_rate.sql` 已创建并在 MySQL 8.0 中执行成功。
- 阶段 2 已完成并通过检查：10 张核心表存在，管理员账号存在且密码非明文，3 条真实线路站点数量为 27、17、32，模拟数据规模为 30 辆车、30 名司机、3000 条班次、10000 条乘车记录，核心统计 SQL 返回合理结果。
- 已完成阶段 3 的 T3.1 Spring Boot 后端项目创建：`backend/` 已创建 Maven Wrapper、Spring Boot 入口、基础配置和健康检查接口，项目可打包，后端可启动，`/api/health` 已验证 MySQL 与 Redis 均为 `up`。
- 已完成阶段 3 的 T3.2 通用基础能力：已实现统一响应结构、统一异常处理、分页请求/响应模型和基础时间格式配置，`/api/debug/page` 已验证成功响应和参数校验错误响应。
- 已完成阶段 3 的 T3.3 管理员登录鉴权：`wjhadmin` / `123456` 可登录，错误密码返回可读错误，未登录访问后台接口返回 `401`，健康检查等公开接口不需要登录，管理员姓名已确认按 UTF-8 正常返回 `王家豪`。
- 已完成阶段 3 的 T3.4 公开线路和地图接口：已实现 `GET /api/public/routes`、`GET /api/public/routes/{id}`、`GET /api/public/routes/{id}/stations`，验证返回 3 条线路，站点数量为 27、17、32，站序连续，经纬度字段可用于地图绘制。
- 已完成阶段 3 的 T3.5 后台基础数据管理接口：已实现后台首页基础统计、线路/站点/车辆/司机/班次基础管理接口和乘车记录分页查询，验证未登录后台接口返回 `401`，登录后可访问统计和分页接口，车辆新增与停用流程可调用，验证数据已清理。
- 已完成阶段 3 的 T3.6 客流统计接口：公开统计路径统一为 `/api/public/statistics/*` 并兼容 `/api/public/stats/*`，线路日客流、站点排行、时段对比和满载率接口已验证可返回数据库统计结果。
- 已完成阶段 3 的 T3.7 调度建议接口：后台可登录后调用 `/api/admin/dispatch-advice/generate` 生成规则建议，AI 服务不可用时返回降级说明，并保存 `dispatch_advice` 记录。
- 已完成阶段 3 的 T3.8 AI 服务客户端：后端封装 Python AI 服务调用，公开 AI 分析和调度建议均具备服务不可用降级逻辑。
- 阶段 3 已完成并通过检查：后端可启动，管理员登录正常，游客公开接口正常，后台基础数据接口正常，统计接口正常，调度建议接口可调用并具备降级逻辑，OpenAPI 文档可访问。
- 已完成阶段 4 的 T4.1 FastAPI 项目创建：`ai-service/` 已包含 FastAPI 应用、依赖文件和健康检查接口。
- 已完成阶段 4 的 T4.2 AI 输入输出 Schema：已定义客流分析和调度建议请求/响应结构。
- 已完成阶段 4 的 T4.3 客流分析接口：`POST /ai/analyze-flow` 可基于后端统计数据返回分析摘要、关键发现、趋势和建议。
- 已完成阶段 4 的 T4.4 调度建议接口：`POST /ai/dispatch-advice` 可基于规则结果和统计数据返回建议、原因、风险和优先级。
- 已完成阶段 4 的 T4.5 AI 降级策略：未配置大模型 Key 时返回模板化分析，项目仍可演示。
- 阶段 4 已完成并通过检查：AI 服务可启动，`/health`、`/ai/analyze-flow`、`/ai/dispatch-advice` 均验证成功，Java 后端可调用 AI 服务。
- 已完成阶段 5 的 T5.1 Vue 3 项目创建：`frontend/` 已包含 Vite、Vue 3、TypeScript、Element Plus、ECharts、路由和请求封装。
- 已完成阶段 5 的 T5.2 首页地图：可加载线路列表和站点数据，配置腾讯地图 Key 时加载腾讯地图，Key 缺失时显示站点走向降级视图。
- 已完成阶段 5 的 T5.3 线路详情：可展示线路基础信息、站点顺序、地图走向和近 7 日客流趋势。
- 已完成阶段 5 的 T5.4 客流分析：可按线路和日期范围展示日客流趋势、站点排行和高峰对比图表。
- 已完成阶段 5 的 T5.5 AI 助手：可提交问题、线路和时间范围，并展示后端/AI 服务分析结果。
- 已完成阶段 5 的 T5.6 管理员登录页：支持 `wjhadmin` 登录并保存 token。
- 已完成阶段 5 的 T5.7 后台首页：可展示基础数据统计和后台入口。
- 已完成阶段 5 的 T5.8 基础数据管理：可在单页 Tabs 中管理线路、站点、车辆、司机、班次，并查询乘车记录。
- 已完成阶段 5 的 T5.9 调度建议管理：可选择线路、日期和时段生成建议并查看建议记录。
- 阶段 5 已完成并通过检查：`npm run build` 通过，前端开发服务器可启动，8 个路由均可访问；当前环境无浏览器二进制，地图/图表视觉检查留在用户浏览器或可用浏览器环境验证。
- 已完成阶段 6 的 T6.1 游客线路与地图流程联调：公开线路、线路站点和前端首页路由可访问，地图 Key 缺失时有降级视图。
- 已完成阶段 6 的 T6.2 客流分析流程联调：前端构建通过，后端统计接口返回真实数据库统计结果，空日期范围返回空数组。
- 已完成阶段 6 的 T6.3 游客 AI 助手流程联调：前端 AI 页面可访问，Java 后端可整理统计数据并调用 Python AI 服务。
- 已完成阶段 6 的 T6.4 管理员后台流程联调：管理员登录、后台统计、基础数据新增/修改/停用、调度建议生成均已验证；测试车辆数据已清理。
- 已完成阶段 6 的 T6.5 异常和降级流程联调：AI 服务不可用时 Java 后端返回规则降级分析，未登录后台接口返回 `401`，地图 Key 缺失时前端显示降级地图。
- 已完成阶段 6 的 T6.6 可运行交付检查：Docker、Java 后端、Python AI 服务、Vue 前端均可启动，README 已更新完整启动顺序。
- 阶段 1 到阶段 6 已完成并通过检查，项目进入阶段 7 用户自测、截图、验收和报告整理。
